namespace Site.UI.Config.Command
{
    public static class CommandConfig
    {
        public static void ConfigCommand(this IServiceCollection services)
        {
            //services.AddTransient<ICommand, Command>();
        }
    }
}
